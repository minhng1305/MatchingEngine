{
  "buildCommand": "cd trading-frontend && npm install && npm run build",
  "outputDirectory": "trading-frontend/build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
